{
  "name": "jev-mail",
  "version": "0.1.0",
  "description": "TypeSafe AI Jev-powered Zero-Inbox email triage automation",
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate.js",
    "build": "node scripts/generate.js && tsc",
    "simulate": "node scripts/generate.js && tsc && node dist/cli.js",
    "test": "npm run simulate"
  },
  "keywords": ["typesafe", "jev", "gmail", "inbox-zero", "system-one"],
  "author": "Vynn Lee",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
